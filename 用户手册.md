# 用户手册 · 数学手写作业分步批改系统

本手册面向系统的**最终使用者**（学生、教师、管理员），说明如何安装、启动系统，以及三种角色各自的典型操作流程。代码层面的实现细节请参阅同目录下的 `代码说明文档.md`。

---

## 目录

- [一、系统简介](#一系统简介)
- [二、运行环境与安装](#二运行环境与安装)
- [三、启动系统](#三启动系统)
- [四、首次使用：创建管理员](#四首次使用创建管理员)
- [五、学生使用指南](#五学生使用指南)
- [六、教师使用指南](#六教师使用指南)
- [七、管理员使用指南](#七管理员使用指南)
- [八、常见问题](#八常见问题)
- [九、数据备份与维护](#九数据备份与维护)

---

## 一、系统简介

本系统是一个面向中小学数学作业的**分步批改与错题归因**平台，主要能力：

- **手写作业 OCR**：手机拍作业上传，自动识别题目与解题步骤（LaTeX 渲染）。
- **分步评分**：逐步判分、给出错因（计算/符号/变量/逻辑/其他），并标注错在哪一步。
- **错题本**：自动收集错题，可添加笔记，按知识点聚合复习。
- **班级管理**：教师创建班级、生成邀请码；学生扫码进班；班级报表一目了然。
- **知识图谱定制**：管理员维护全局图谱；教师可在班级层级覆盖节点。
- **三级角色**：学生、教师、管理员，权限逐级递增。

---

## 二、运行环境与安装

### 2.1 硬件与操作系统

- Windows / macOS / Linux 均可。
- 需要联网（调用云端大模型 OCR 与评分服务）。
- 内存 ≥ 8 GB；磁盘空间 ≥ 5 GB（含数据集与数据库）。

### 2.2 软件依赖

- **Python 3.10+**（建议 3.11）
- 浏览器：Chrome / Edge / Safari（需支持 ES2017+）

### 2.3 安装步骤

进入项目根目录：

```bash
cd "Step-based scoring system for mathematics"
```

创建并激活虚拟环境：

```bash
python3 -m venv .venv
source .venv/bin/activate          # macOS / Linux
# Windows: .venv\Scripts\activate
```

安装依赖：

```bash
pip install -r requirements.txt
```

### 2.4 配置 `.env`

复制 `.env.example` 为 `.env` 并填入：

```dotenv
# 大模型评分（留空则只用规则评分）
LLM_API_KEY=sk-xxxxxxxx
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_MODEL=deepseek-chat
LLM_SCORE_TIMEOUT_SEC=90

# 认证与数据库
AUTH_SECRET=请改成一段随机字符串
AUTH_EXP_MINUTES=1440
DB_PATH=outputs/app.db

# 上传大小上限（MB）
MAX_UPLOAD_MB=10
```

> 视觉 OCR 与评分走两路模型：`mimo-v2.5` 负责识别手写图片，`deepseek-v4-pro` 负责复查与评分。模型选择在 `app/config.py` 中配置。

---

## 三、启动系统

在虚拟环境中运行：

```bash
uvicorn app.main:app --reload --port 8001
```

看到类似 `Uvicorn running on http://127.0.0.1:8001` 即成功。

浏览器访问：**http://127.0.0.1:8001**

### 停止服务

在终端按 `Ctrl + C`。若端口被占用：

```bash
# macOS / Linux
pkill -9 -f "uvicorn app.main"
```

---

## 四、首次使用：创建管理员

系统**首位管理员**通过自助注册产生，之后管理员角色即关闭自助注册。

1. 打开 `http://127.0.0.1:8001`，在登录区点击「注册」。
2. 用户名 ≥ 3 位，密码 ≥ 6 位。
3. 此时系统检测到尚无管理员，会自动放出「管理员」角色单选项。选中并注册。
4. 注册成功后自动登录并跳转至 `/admin` 后台。

> 之后注册的账号只能是「学生」或「教师」。若需要新增管理员，必须由现任管理员在后台手动改角色。

---

## 五、学生使用指南

学生是系统的**作业提交方**，主要在首页（`/`）活动。

### 5.1 注册与登录

- 注册时角色选「学生」。
- 登录后进入首页，可看到「上传作业」「历史记录」「错题本」「我的班级」。

### 5.2 加入班级

1. 向老师索取 **6 位邀请码**（如 `ABCD23`）。
2. 点击「我的班级」→「加入班级」，输入邀请码。
3. 加入成功后，该老师即可看到你的作业数据与错题。

> 一位学生可同时加入多个班级。

### 5.3 提交作业批改

1. 点击「上传作业」，选择拍好的作业照片（JPG/PNG，≤ 10 MB）。
2. 系统识别后会按题分页展示，可检查识别是否准确。
3. 点击「评分」：
   - 默认对所有题评分；
   - 也可只评当前题。
4. 评分完成后可看到：
   - 每一步的得分、对错、反馈；
   - 错误步骤会用**红色高亮**，并标出错因（计算/符号/变量/逻辑/其他）；
   - 总分、总耗时、诊断信息。

### 5.4 错题本

- 顶部菜单「错题本」自动汇总所有做错的题。
- 点击一条记录可展开：
  - 题目与每一步；
  - 错在红色的那一步；
  - 关联的知识点（来自知识图谱）。
- 可为每道错题写**笔记**（点击「编辑笔记」直接在行内修改，**不是**弹窗）。

### 5.5 历史与收藏

- 「历史记录」查看所有批改过的作业。
- 对典型题可点击「收藏」便于以后复习。

---

## 六、教师使用指南

教师是**班级管理者**，登录后自动跳转到 `/admin`（教师后台）。

### 6.1 后台结构

教师后台仅包含与教学相关的菜单：

- **我的班级**：创建、编辑、查看报表。
- **班级学生**：查看本班成员及批改数据。
- **知识图谱（班级层）**：为本班定制知识点。

「系统概览」「全局用户」等菜单**仅管理员可见**。

### 6.2 创建班级

1. 「我的班级」→「新建班级」。
2. 填写：班级名称（必填）、学段（小学/初中/高中）、年级、简介。
3. 确定后会生成一个 **6 位邀请码**（去除易混淆字符 0/O/1/I）。
4. 把邀请码告诉学生，学生用它加入。

> 若邀请码泄露，可在班级编辑页点「重置邀请码」换新码（旧码失效）。

### 6.3 查看班级报表

「我的班级」→ 对应班级「查看报表」：

- 成员名单、人数；
- 班级累计批改次数、平均分；
- 最近 10 次批改明细；
- 错因分布柱状图（计算/符号/变量/逻辑/其他）。

### 6.4 管理班级学生

- 「班级学生」显示你所教的所有班级的学生。
- 可按班级筛选。
- 学生信息只读，不能删除（学生退班由学生本人操作；若需强制清理，联系管理员）。

### 6.5 班级知识图谱定制

管理员维护一份**基础知识图谱**，教师可在此基础上为班级做定制：

1. 「知识图谱」→ 选择班级。
2. 对每个节点（知识点）可选择：
   - **保留**（默认使用基础节点）；
   - **覆盖**（修改名称/描述/难度/先修）；
   - **隐藏**（本班不显示该节点）。
3. 保存后，本班学生看到的错题知识点就是定制后的版本。

> 定制只影响本班级；基础图谱与其他班级不受影响。

### 6.5 退出登录

教师后台**右上角**有「退出登录」按钮，点击后回到首页。

---

## 七、管理员使用指南

管理员（admin）是**系统层面**的管理者，权限最高。

### 7.1 首位管理员创建

见 [第四节](#四首次使用创建管理员)。

### 7.2 后台菜单

管理员拥有全部菜单：

- **系统概览**：用户数、班级数、批改总量等。
- **用户管理**：所有学生/教师/管理员账号；可改角色、重置密码、停用。
- **班级管理**：系统全部班级；创建时可指派任意教师为创建者。
- **基础知识图谱**：维护全局节点（增/改/删/连边）。

### 7.3 典型操作

#### 指派教师为班级创建者

1. 「班级管理」→「新建班级」。
2. 「创建教师」下拉中选择某位教师。
3. 该班级将以这位教师身份出现在 ta 的后台。

#### 修改用户角色

- 「用户管理」→ 对应用户「编辑」。
- 可在 student/teacher/admin 间切换；提升为 admin 需谨慎。

#### 维护基础知识图谱

- 「基础知识图谱」以可视化方式编辑。
- 节点字段：`name` 名称、`description` 描述、`difficulty` 难度、`prerequisites` 先修节点。
- 改动立即对全系统生效（教师的班级覆盖层仍优先生效）。

### 7.3 退出登录

后台右上角「退出登录」按钮。

---

## 八、常见问题


### Q2：评分时显示「LLM 失败，回退规则评分」

大模型评分超时或服务异常，系统会自动用规则评分兜底，结果仍可用。若长期回退，检查：
- `.env` 中的 `LLM_API_KEY` 与 `LLM_BASE_URL` 是否正确；
- 网络是否能访问该 API；
- `LLM_SCORE_TIMEOUT_SEC` 可适当调大。

### Q3：学生加入班级提示「邀请码无效」

- 确认邀请码 6 位、无空格；
- 教师可能已「重置邀请码」，请重新索取；
- 邀请码字母大写，输入时系统会自动转大写。

### Q4：教师登录后被强制跳转后台，看不到学生首页

这是设计如此：**教师只需后台**，不需要学生端的作业上传界面。如需体验学生端，请用学生账号登录。

### Q5：管理员角色注册项不见了

系统一旦存在任意一位管理员，自助注册管理员的入口就会关闭。新增管理员必须由现任管理员在后台「用户管理」中改角色。

### Q6：退出登录后停在后台页

正常情况下退出会自动回到首页（`/`）。若停留在后台，刷新一次或手动访问根路径即可。

### Q7：数据库迁移 / 重置

数据库位于 `outputs/app.db`。
- **备份**：直接复制该文件。
- **重置**：停掉服务后删除该文件，下次启动会自动新建空库（原有数据全部丢失，谨慎操作）。

### Q8：端口 8001 已被占用

```bash
# 查看占用进程
lsof -i :8001
# 强制释放
pkill -9 -f "uvicorn app.main"
```

或换端口启动：`uvicorn app.main:app --reload --port 8002`。

---

## 九、数据备份与维护

### 9.1 关键文件

| 路径 | 说明 |
|---|---|
| `outputs/app.db` | 全部业务数据（用户、班级、批改、错题、图谱） |
| `outputs/ocr_records` | OCR 识别记录与原图缓存 |
| `.env` | 大模型密钥与系统配置 |
| `outputs/llm_failures` | LLM 失败诊断日志（排查 OCR/评分异常时使用） |

### 9.2 日常备份

```bash
# 停服后执行
cp outputs/app.db "outputs/app.backup.$(date +%Y%m%d).db"
```

### 9.3 升级依赖

```bash
source .venv/bin/activate
pip install -r requirements.txt --upgrade
```

### 9.4 日志位置

- 服务日志：终端窗口（或 systemd/journalctl，若部署为服务）。
- 业务异常：`outputs/llm_failures/` 下的分阶段 JSON 文件。

---

## 附录：快捷命令速查

```bash
# 激活环境
source .venv/bin/activate

# 启动服务
uvicorn app.main:app --reload --port 8001

# 强制释放端口
pkill -9 -f "uvicorn app.main"

# 备份数据库
cp outputs/app.db outputs/app.backup.db

# 查看数据库（需安装 sqlite3 CLI）
sqlite3 outputs/app.db ".tables"
```
