# 代码说明文档 · MathGrade 数学作业步骤批改系统

> 文档版本：2026-06-22（结题版本）

本系统的核心目标是**按步骤批改数学作业**：拍/上传作业图片 → OCR 识别 → 切题切步骤 → 逐步打分 → 自动入库错题、更新知识点掌握度、生成精确报告。围绕这条主线，系统还提供三角色（学生 / 教师 / 管理员）+ 班级管理 + 知识图谱可视化 + 六轮评测套件。

---

## 1. 技术栈

| 层 | 选型 |
|---|---|
| 后端框架 | FastAPI（异步 ASGI，Pydantic 校验） |
| 数据存储 | SQLite（`outputs/app.db`），无外部数据库依赖 |
| 视觉 OCR | 多模态 LLM（生产用 `mimo-v2.5`，兼容 OpenAI Vision API） |
| 文本 OCR / 评分 / 纠错 | 文本 LLM（生产用 `deepseek-v4-pro`，走 OpenAI-compatible API） |
| 备用 OCR | cnocr + latex-ocr + Pix2Text（本地小模型，可离线） |
| 前端 | 原生 HTML + CSS + JavaScript（SPA 结构，无构建工具） |
| 数学渲染 | KaTeX |
| 知识图谱可视化 | vis-network |
| 图像处理 | OpenCV（deskew / denoise / 版面分析） |

三方 LLM 凭证通过 `.env` 配置，三个模型可分别指向不同供应商：
- `LLM_VISION_*` 视觉 OCR 模型
- `LLM_SCORE_*` 评分 / 文本纠错模型
- `LLM_*` 兜底默认值

---

## 2. 目录结构总览

```
Step-based scoring system for mathematics/
├── app/                         后端 Python 包
│   ├── main.py                  FastAPI 入口，所有 HTTP 路由
│   ├── config.py                从 .env 加载的运行时配置
│   ├── schemas.py               Pydantic 请求/响应模型
│   └── services/                业务服务层
│       ├── auth.py              密码哈希 + HMAC token
│       ├── db.py                SQLite 表结构与 CRUD
│       ├── ocr_service.py       多引擎本地 OCR 流水线
│       ├── vision_ocr.py        ★ 纯视觉 LLM OCR（主力路径）
│       ├── ocr_corrector.py     文本 LLM 纠错
│       ├── vision_corrector.py  视觉 LLM 纠错
│       ├── ocr_postprocess.py   OCR 片段分类 / 题号切分 / 阅读顺序
│       ├── step_parser.py       文本切步骤
│       ├── question_splitter.py 题号识别
│       ├── layout_segmenter.py  OpenCV 版面分析
│       ├── image_preprocess.py  图像去噪 / 纠斜
│       ├── scorer.py            ★ 步骤评分（规则 + LLM + 反思）
│       ├── knowledge_graph.py   K-12 知识图谱本体 + 班级覆盖层
│       ├── classes.py           班级管理（邀请码 / 角色 / 报表）
│       └── report_generator.py  精确报告异步生成
├── static/                      前端资源（SPA）
│   ├── index.html               页面骨架
│   ├── app.js                   ★ 路由 + 业务逻辑 + API 调用
│   ├── kg.js                    知识图谱可视化（vis-network）
│   └── styles.css               设计系统 + 组件样式
├── data/
│   └── kg_ontology.yaml         基础 KG（40 个 K1-K12 知识点，可版本化）
├── datasets/                    ★ 评测数据集统一索引（符号链接）
├── evaluation/                  六轮评测套件（v1-v6）
│   ├── v1_scoring/              步骤评分准确率
│   ├── v2_detection/            错误检测 v2
│   ├── v3_detection/            错误检测 v3
│   ├── v4_ocr/  v4b_ocr/  v4c_ocr/  手写公式 OCR
│   ├── v5_aida/                 微积分手写 OCR
│   ├── v5_im2latex/             印刷公式 OCR
│   └── v6_crohme/               CROHME 在线手写 OCR
├── outputs/                     运行时产物（app.db、上传图、报告等）
├── transcriptions/              小红书 OCR 转写参考样本
├── presentation/                答辩 slides.md
├── requirements.txt
├── .env.example
└── README.md
```

★ = 核心模块，下面分节详述。

---

## 3. 后端模块详解（`app/`）

### 3.1 `app/main.py` —— FastAPI 入口

承担三层职责：

1. **应用初始化**：建 FastAPI 实例、挂载 `/static`、调用 `init_db()` 建表与迁移。
2. **认证依赖**：`_current_user()` 解析 Bearer token，注入当前用户到路由；401 触发前端登录弹窗。
3. **业务路由**：按职责分组——

| 路径前缀 | 用途 | 角色限制 |
|---|---|---|
| `/api/auth/*` | 注册、登录、当前用户、bootstrap 状态 | 公开 |
| `/api/history/*` `/api/favorites` `/api/wrong-answers*` | 学生个人中心数据 | 学生本人 |
| `/api/ocr*` `/api/grade` | OCR + 评分 | 任何登录用户 |
| `/api/classes*` | 班级：学生加入/退出，通用查询 | 学生+教师 |
| `/api/admin/*` | 用户管理、班级管理、基础 KG | 教师+管理员 |
| `/api/teacher/*` | 教师本班范围（学生、班级 KG 覆盖层） | 教师+管理员 |
| `/api/kg/*` | 知识图谱本体与掌握度 | 任何登录用户 |

**关键端点**：
- `POST /api/ocr-vision-only` — 主力 OCR 端点，返回 `{ocr_text, problems:[{qno,question_text,step_lines}, ...]}`
- `POST /api/grade` — 评分入口，支持 `step_lines` 直传（绕过文本切题）
- `POST /api/admin/classes` — 创建班级，admin 可指定 `creator_id` 把班级挂给某教师
- `GET /api/teacher/classes/{id}/kg` — 返回「基础 KG + 本班 override」的合并结果

### 3.2 `app/config.py` —— 配置加载

启动时 `_load_dotenv()` 读工作目录（或上级）的 `.env`，**不覆盖已存在的环境变量**（方便 Docker / CI 注入）。`Settings` 是 dataclass，集中暴露所有配置项，分四组：
- **LLM 凭证**：`llm_*` / `llm_vision_*` / `llm_score_*` 三套独立配置，可指向不同供应商。
- **网络超时**：评分模型慢，`llm_score_*` 允许独立配置；`llm_score_no_read_timeout=true` 完全关读超时。
- **上传/存储**：`max_upload_mb`、`db_path`。
- **特性开关**：`kg_llm_enabled`、`llm_score_reflection_enabled`。

### 3.3 `app/schemas.py` —— Pydantic 模型

API 的数据契约：
- `StepItem` 单步（含 `normalized`、`has_equation`、`confidence`）
- `StepScore` 单步打分
- `QuestionGrade` 单题（题干 + 步骤 + 步骤分 + 反馈）
- `GradingResult` 整卷（多题 + 总分 + 元信息）
- `DetailReportResponse` 精确报告轮询响应
- `AuthUser` / `AuthResponse` 登录响应（`role` 三档：student/teacher/admin）

### 3.4 服务层（`app/services/`）

#### `auth.py` —— 零依赖认证
密码：SHA-256 + 8 字节随机盐，存 `salt$digest`。Token：自签 `<base64url(payload)>.<hex_sig>`，payload 含 `uid/username/role/exp`，用 `settings.auth_secret` 做 HMAC-SHA256。刻意不用 `passlib` / `python-jose`，最小化部署依赖。

#### `db.py` —— SQLite 持久化
所有 SQL 集中在此。业务层只见 Python 函数。关键表：
- `users(id, username, password_hash, role)`
- `ocr_records` / `grading_records`（含 `detail_report_md` 异步生成的精确报告）
- `wrong_answers`（含 `steps_json` 存逐步结构化数据）
- `favorite_assignments`（收藏）
- `classes` / `user_classes`（班级与多对多关联）
- `class_kg_overrides`（教师为本班定制的 KG 覆盖层）
- `llm_failures`（LLM 故障日志，仅教师/管理员可见）

`init_db()` 启动时跑幂等迁移：`ALTER TABLE ADD COLUMN` 会被 `PRAGMA table_info` 检查跳过，老 DB 自动升级。

`populate_wrong_answers_from_session()` 每次批改后自动把失分题入库，并为每步生成结构化记录（`text/reason/score/max_score/wrong`），前端据此高亮具体失分步骤。

#### `vision_ocr.py` —— ★ 主力 OCR
两阶段：
1. **第一阶段（视觉 LLM）**：原图 base64 喂给多模态模型，直接输出结构化 JSON（题干 + 步骤），已完成 Unicode→LaTeX 归一化。
2. **第二阶段（文本 LLM review）**：`review_ocr_with_text_llm()` 把第一阶段所有行丢给更强的文本模型，重排序、丢弃噪声、按题组织。

返回给前端：`{ocr_text, question_text, steps_text, problems:[{qno,question_text,step_lines}, ...]}`。`problems` 字段是核心创新——前端和 scorer 直接消费它，避免了「题干被当步骤打分」的历史问题。

**严格抄录约束**：prompt 第 8-9 条明确禁止 LLM 自行补全答案（例如学生停在 `= 2 ln(x+1)|_0^1`，禁止 LLM 在 s 里追加 `= 2 ln 2`）。配合 `scorer._step_appears_in_corpus` 的相似度检测做二次过滤。

#### `scorer.py` —— ★ 步骤评分
两条路径：
- **LLM 路径**：`_llm_score_steps` 用 `deepseek-v4-pro` 给每步打分 + 写扣分理由；可选第二轮「反思」让 LLM 自检。失败/超时/JSON 解析错自动降级到规则法。
- **规则路径**：`_rule_score_steps` 用正则 + 公式相似度兜底，保证任何情况都有分数输出。

两个对外入口：
- `score_questions(ocr_text, ...)` — 老路径，内部切题切步骤
- `score_one_problem(qno, question_text, step_lines, ...)` — 新路径，消费 review 已分好的单题，避免题干被当步骤打分

辅助：
- `_step_appears_in_corpus` 用 `SequenceMatcher.ratio() >= 0.75` 检测脑补步骤
- `_invoke_score_llm_with_retry` 指数级延长超时的重试包装（`TIMEOUT_MULTIPLIERS = (1.0, 1.5, 2.0, 3.0)`）

#### `knowledge_graph.py` —— K-12 知识图谱
- 基础 KG：`data/kg_ontology.yaml`（40 个 K1-K12 节点，可版本化），加载到 NetworkX DiGraph 单例。
- 角色：admin 改基础 KG（写回 YAML）；teacher 在基础之上加**班级覆盖层**（`class_kg_overrides` 表：upsert 新节点 / delete 隐藏基础节点）。
- `get_effective_class_nodes(class_id)` = 基础 ∪ upserts − deletes。
- `enrich_report_with_kg` 把批改结果按步映射到知识点；`compute_user_mastery` 聚合用户历史算每个节点的掌握度。
- `infer_error_type` 从批改理由里抽错因（calculation/sign/variable/logic/other）。

#### `classes.py` —— 班级管理
- 邀请码：`_new_invite_code()` 生成 6 位无歧义（去掉 `0/O/1/I`）邀请码，碰撞重试。
- 角色作用域：`list_classes(user_id, role)` 按角色返回不同范围（admin 全部、teacher 自己 creator_id 的、student 自己 join 过的）。
- `is_teacher_of_class` / `list_class_member_ids` 被 main.py 的权限层调用。
- `get_class_report` 聚合成员、批改次数、平均分、错因分布，是教师后台「班级 → 查看报表」的数据源。

#### 其他服务文件

| 文件 | 作用 |
|---|---|
| `ocr_service.py` | 多引擎本地 OCR（cnocr + latex-ocr 或 Pix2Text），作为视觉 LLM 的离线 fallback |
| `ocr_corrector.py` | 文本 LLM 二次纠错（只看文本，便宜） |
| `vision_corrector.py` | 视觉 LLM 二次纠错（带原图，贵但准） |
| `ocr_postprocess.py` | OCR 片段分类、低置信打标、题号切分、双栏阅读顺序 |
| `step_parser.py` | 把整段文本切成 `StepItem` 列表 |
| `question_splitter.py` | 题号识别（`第 N 题` / `N.` / `N、`） |
| `layout_segmenter.py` | OpenCV 版面分析，切出文字/公式区域 |
| `image_preprocess.py` | 图像去噪 / 纠斜 / 二值化 |
| `report_generator.py` | 异步生成精确报告 Markdown，状态机 `pending → ready / failed` |

---

## 4. 前端模块详解（`static/`）

### 4.1 `index.html` —— SPA 骨架

单 HTML 文件，含全部页面骨架：
- 顶部导航（`作业批改 / 个人中心 / 教师后台`，按角色显示）
- 4 个 `page-view` 区块（grading / account / admin / 模态框组）
- 4 步批改进度条 `taskStepper`
- 两个主要模态框：认证（登录/注册）、精确报告预览；班级编辑/报表模态框；错题笔记内联编辑器

### 4.2 `app.js` —— ★ 前端控制器

~3600 行，承担全部交互行为。三大块：
1. **路由**（`routePage`）：根据 URL pathname 切 `page-view`，处理角色权限（学生看作业批改 + 个人中心；教师/管理员强制跳到后台）。
2. **业务流水线**：
   - **批改流水线**：`assignmentPages` 多页 OCR、问题切分、逐步评分、恢复历史
   - **错题本**：`loadWrongAnswers` 含逐步 KaTeX 渲染 + 失分步骤高亮 + 内联笔记编辑（替代 `prompt()`）
   - **知识图谱**：依赖 `kg.js` 的 vis-network 可视化
   - **收藏 / 历史 / 班级 / 后台**
3. **后台**（admin）：三角色共享同一外壳，按角色动态隐藏 tab——
   - admin：概览 / 用户管理 / 班级 / 基础 KG
   - teacher：班级 / 班级 KG 定制（无概览、无全用户）

**约定**：`$(id)` 简写；所有后端调用走 `apiFetchJson` 自动带 Authorization；状态消息走 `setStatus(text, kind)` 显示 toast。

### 4.3 `kg.js` —— 知识图谱可视化

- `initKgPage()` 拉 `/api/kg/ontology` + `/api/kg/mastery`，用 vis-network 渲染。
- 节点颜色按掌握度：红（薄弱）/ 黄（待巩固）/ 绿（掌握）/ 灰（未测）。
- `showNodeDetail(nodeId)` 点击节点显示右侧面板（前置知识点、关键词、错因提示）。

### 4.4 `styles.css` —— 设计系统

~2300 行。颜色/圆角/间距全部定义为 `:root` 上的 CSS 变量，组件层只引用变量。色板：中性灰为主 + 蓝（主操作）/ 橙（警告、失分）/ 绿（成功、掌握）/ 红（错误、删除）。按「基础 → 组件 → 页面 → 角色」分节。

---

## 5. 数据集说明（`datasets/`）

`datasets/` 目录是所有评测数据集的**统一索引**，里面是指向 `evaluation/<套件>/data/` 的符号链接。所有数据集均来自公开 HuggingFace 仓库，下载脚本在 `evaluation/<套件>/download_dataset.py`（或 `download_math.py`），可重跑下载。

### 5.1 数据集一览

| 目录 | 套件用途 | 数据来源 | 样本量 | 用途 |
|---|---|---|---|---|
| `datasets/v1_scoring_math/` | 步骤评分准确率 | HF `EleutherAI/hendrycks_math`（MATH 数据集 Level 1-3） | 200（按难度分层 40/80/80） | 测评分模型在多步解题上的 F1 / 准确率 |
| `datasets/v2_detection_math/` | 错误检测 v2（基线） | 同上 MATH + 本地错误注入 | 200（15% 注入错误） | 测二分类「这步对吗」的 precision/recall |
| `datasets/v3_detection_math/` | 错误检测 v3（改进 prompt） | 同 v2 | 同 v2 | 对比 v2，测 prompt 改进带来的增益 |
| `datasets/v4_ocr_mathwriting/` | 手写公式 OCR | HF `deepcopy/MathWriting-human` | 100 | 测 OCR 在真实手写公式上的 LaTeX 还原率 |
| `datasets/v4b_ocr_mathwriting/` `v4c_ocr_mathwriting/` | v4 OCR 的迭代对照 | 同 v4 | 同 v4 | 测不同 prompt / 纠错策略的对比 |
| `datasets/v5_aida_calculus/` | 大学微积分手写 | HF `deepcopy/Aida-Calculus-Math-Handwriting` | 100 | 测 OCR 在更高难度（极限/积分）上的表现，也是 OOD 样本 |
| `datasets/v5_im2latex/` | 印刷公式 OCR | HF `yuntian-deng/im2latex-100k`（test split） | 100 | 测 OCR 在排版印刷公式上的上限（baseline） |
| `datasets/v6_crohme/` | CROHME 手写 | HF `linxy/LaTeX_OCR` | 100 | 测 CROHME 风格手写公式（更接近作业） |

### 5.2 数据集作用详解

**v1_scoring —— 评分质量评测**
- 数据：200 道 MATH 数据集 Level 1-3 题，每题含题干 + 标准解答步骤。
- 评测方法：把标准解法当作「学生作答」走完整评分流程，期望总分接近满分；用 `inject_errors.py` 生成「错误版本」测错误检测精度。
- 输出：`evaluation/v1_scoring/results/report.md`。

**v2_detection / v3_detection —— 错误检测评测**
- 数据：在 v1 的 MATH 题上注入 4 类错误（数字 / 符号 / 变量 / 插入），15% 控制组保留正确。
- 评测方法：测模型「标出哪步错」的二分类指标。v3 相比 v2 主要改了 prompt 和 detector 实现。
- 输出：`evaluation/v{2,3}_detection/results/report.md`，含 v2↔v3 对比。

**v4 / v4b / v4c —— 手写公式 OCR**
- 数据：100 张 `MathWriting-human` 手写公式图 + LaTeX ground truth。
- 评测方法：用 BLEU / edit distance / 精确匹配率衡量 OCR 输出与 GT 的接近程度。三个版本分别测不同的视觉 LLM prompt / 纠错策略。
- 输出：`evaluation/v4*_ocr/results/report.md`。

**v5_aida —— 微积分手写 OCR（OOD 样本）**
- 数据：100 张 Aida-Calculus 手写微积分公式（极限、积分、级数等大学内容）。
- 用途：测 OCR 在训练分布外的鲁棒性；同时验证 K-12 KG 映射是否会误触发（应该走 out-of-scope 分支）。

**v5_im2latex —— 印刷公式 OCR（上限 baseline）**
- 数据：100 张 im2latex-100k test split 排版公式。
- 用途：印刷公式是最容易的 OCR 任务，分数代表模型上限；手写任务的分数与这里的差距就是「手写难度」。

**v6_crohme —— CROHME 风格手写 OCR**
- 数据：100 张 `linxy/LaTeX_OCR`（CROHME 风格在线手写渲染图）。
- 用途：CROHME 是手写数学识别的标准基准，加入这个数据集让评测结果可与学术界对比。

### 5.3 重新下载

每个数据集都可用对应目录下的 `download_dataset.py` 重新下载，例如：

```bash
.venv/bin/python evaluation/v6_crohme/download_dataset.py
```

脚本会从 HuggingFace Hub 拉取 → 保存到 `evaluation/<套件>/data/images/*.png` + `manifest.json`（含 id、路径、LaTeX GT）。需要先安装依赖：

```bash
.venv/bin/pip install datasets pyarrow pandas pillow
```

---

## 6. 核心数据流

### 6.1 批改流水线

```
用户上传图片
  ↓
POST /api/ocr-vision-only
  ↓
vision_ocr.vision_only_ocr()
  ├─ 第一阶段：视觉 LLM 出 {question_lines, step_lines}
  └─ 第二阶段：review_ocr_with_text_llm() 出 [{q, s}, ...]
  ↓
返回 {ocr_text, problems:[{qno, question_text, step_lines}, ...]}
  ↓
前端展示给用户校对 → 用户点「确认并进入评分」
  ↓
POST /api/grade (带 step_lines / qno)
  ↓
scorer.score_one_problem() 每题独立打分
  ├─ LLM 路径（含反思二轮）
  └─ 失败降级 → 规则路径
  ↓
populate_wrong_answers_from_session() 自动入库失分题
enrich_report_with_kg() 映射知识点
  ↓
BackgroundTask: report_generator.generate_detail_report() 异步生成精确报告
  ↓
返回 GradingResult 给前端
```

### 6.2 角色权限流

```
请求 → _current_user() 解析 token → 拿到 role
  ↓
student → 只能访问自己的资源（user_id 匹配）
  ↓
teacher → 只能访问 creator_id=自己 的班级相关资源
          _assert_teaches_class() 校验每条 /api/teacher/* 请求
  ↓
admin → 继承 teacher 全部权限 + 能操作所有班级、所有用户、基础 KG
```

### 6.3 班级 KG 定制流

```
教师在前端选班级 → 看「基础 KG + 本班 override」合并视图
  ↓
教师新增/编辑节点 → POST /api/teacher/classes/{id}/kg/nodes
  ↓
knowledge_graph.upsert_class_override() 写 class_kg_overrides 表
  ↓
该班学生批改时，KG 映射读 get_effective_class_nodes(班级)
  → 用本班定制版算掌握度、错因归类
  ↓
其他班级、基础 KG 不受影响
```

---

## 7. 部署与运行

### 7.1 安装

```bash
# 1. 建 venv
python3 -m venv .venv
source .venv/bin/activate

# 2. 装依赖
pip install -r requirements.txt

# 3. 配置 .env
cp .env.example .env
# 编辑 .env 填入 LLM_API_KEY 等
```

### 7.2 启动

```bash
.venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000
```

首次启动自动建表、跑迁移。访问 `http://localhost:8000`。

### 7.3 首次创建管理员

系统启动时如果没有 admin 用户，注册页会显示「管理员」单选项，第一个注册的管理员即可使用。之后管理员只能由现有管理员在「后台 → 用户管理」升权。

---

## 8. 评测运行

每个评测套件都是独立 Python 脚本：

```bash
# 评分质量
.venv/bin/python evaluation/v1_scoring/run_eval.py

# 错误检测
.venv/bin/python evaluation/v2_detection/run_eval.py
.venv/bin/python evaluation/v3_detection/run_eval.py

# OCR
.venv/bin/python evaluation/v4_ocr/run_ocr.py
.venv/bin/python evaluation/v5_aida/run_ocr.py
.venv/bin/python evaluation/v5_im2latex/run_ocr.py
.venv/bin/python evaluation/v6_crohme/run_ocr.py
```

结果写到各自 `results/report.md`。详见 `evaluation/*/README`（如果存在）或脚本头部注释。

---

## 9. 后续维护要点

1. **scorer.py 改动需要重跑评测**：任何打分逻辑变更都应该跑 `evaluation/v1_scoring/` 确保 F1 不回退（这是项目的核心质量基线，详见 memory 里的 `feedback_scorer_protection.md`）。
2. **DB 迁移幂等**：新增字段加 `ALTER TABLE` 时，一定要在 `init_db()` 里用 `PRAGMA table_info` 检查跳过，保证老 DB 无损升级。
3. **prompt 改动需要回归**：`vision_ocr.py` 和 `scorer.py` 的 prompt 字符串是核心资产，改之前先想清楚「为什么这么写」（注释里有原因，例如「禁止 LLM 脑补答案」）。
4. **新增 LLM 故障诊断**：`llm_failures` 表的诊断 stage 有约定枚举（`ocr-trace` / `ocr-debug` / `score-llm-fallback` / `score-drop-fabricated`），新增 stage 时同步更新教师后台的查看过滤器。

---

*本文件覆盖项目所有公开模块与数据流。内部实现细节请结合源码注释阅读，每个模块顶部 docstring 都有一段「为什么这么设计」的说明。*
